import { Box } from "@/components/chakra-components";
import EmptyVault from "./EmptyVault";

const VaultId = () => {
  return (
    <Box p={4}>
      <EmptyVault />
    </Box>
  );
};

export default VaultId;
